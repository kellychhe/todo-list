<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>to-do list</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Shadows+Into+Light+Two&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="favicon.ico">
    <link rel="stylesheet" href="font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h1>Get your life together, make a todo list!</h1>

    <form action="/tasks" method="POST">
        <input type="text" placeholder="Enter a task!" name="listItem">
        <button type="submit">Submit</button>
    </form>

    <ul class="tasks">
        <% for(var i=0; i<tasks.length; i++) {%>
            <% if (tasks[i].listItem !== ''){ %>
            <li class="task">
                <span id="<%= tasks[i]._id %>" class="<%= tasks[i].taskDone ? 'crossOut' : '' %>">
                    <%= tasks[i].listItem %>
                </span>
                <span><i class="fa fa-check" aria-hidden="true"></i></span>
                <span><i class="fa fa-trash" aria-hidden="true"></i></span>

            </li>
        <% } %>
        <% } %>
    </ul>
    <!-- the trash gets rid of name and message. the name and message could not be found, li is never built and trash can, thumbs upand down are not put in DOM -->
    <span>Tasks Left: <%= taskCount %></span>
       
    <div>
        <button id="removeAll">Remove All</button>
        <button id="clearCompleted">Clear Completed</button>
    </div>
    <script src="main.js"></script>

</body>

</html>